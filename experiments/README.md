# Experiments

This directory contains stable experiment entry points and run notes.

Suggested naming:

- `001_data_audit_nasa/`
- `002_baseline_lstm/`
- `003_physics_informed_loss/`
- `004_pi_tnet_reproduction/`

Exploratory notebooks belong in `notebooks/`; reusable code belongs in `src/battery_rul/`.
