# Model Artifacts

This directory stores trained model artifacts only.

- `checkpoints/`: training checkpoints.
- `exports/`: final exported models for evaluation or thesis reproduction.

Source code for neural-network architectures belongs in `src/battery_rul/models/`.
